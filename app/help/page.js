import HelpClient from './help-client';

export const metadata = {
    title: 'Help',
    description: 'In-app user manual and help guide for PokeDexter.',
};

export default function HelpPage() {
    return <HelpClient />;
}
